find_program(
  clang-cl
  NAMES clang-cl
  PATHS "C:/Program Files/LLVM/bin"
  NO_DEFAULT_PATH
)

find_program(
  clang-cl
  NAMES clang-cl
  REQUIRED
)

find_program(
  llvm-lib
  NAMES llvm-lib
  PATHS "C:/Program Files/LLVM/bin"
  NO_DEFAULT_PATH
)

find_program(
  llvm-lib
  NAMES llvm-lib
  REQUIRED
)

find_program(
  llvm-nm
  NAMES llvm-nm
  PATHS "C:/Program Files/LLVM/bin"
  NO_DEFAULT_PATH
)

find_program(
  llvm-nm
  NAMES llvm-nm
  REQUIRED
)

find_program(
  llvm-objdump
  NAMES llvm-objdump
  PATHS "C:/Program Files/LLVM/bin"
  NO_DEFAULT_PATH
)

find_program(
  llvm-objdump
  NAMES llvm-objdump
  REQUIRED
)

find_program(
  llvm-ranlib
  NAMES llvm-ranlib
  PATHS "C:/Program Files/LLVM/bin"
  NO_DEFAULT_PATH
)

find_program(
  llvm-ranlib
  NAMES llvm-ranlib
  REQUIRED
)

find_program(
  llvm-mt
  NAMES llvm-mt
  PATHS "C:/Program Files/LLVM/bin"
  NO_DEFAULT_PATH
)

find_program(
  llvm-mt
  NAMES llvm-mt
  REQUIRED
)

find_program(
  llvm-strip
  NAMES llvm-strip
  PATHS "C:/Program Files/LLVM/bin"
  NO_DEFAULT_PATH
)

find_program(
  llvm-strip
  NAMES llvm-strip
  REQUIRED
)

find_program(
  llvm-rc
  NAMES llvm-rc
  PATHS "C:/Program Files/LLVM/bin"
  NO_DEFAULT_PATH
)

find_program(
  llvm-rc
  NAMES llvm-rc
  REQUIRED
)
