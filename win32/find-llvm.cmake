find_program(
  clang-cl
  NAMES clang-cl
  PATHS "C:/Program Files/LLVM/bin"
  NO_DEFAULT_PATH
)

find_program(
  clang-cl
  NAMES clang-cl
  REQUIRED
)

find_program(
  llvm-lib
  NAMES llvm-lib
  PATHS "C:/Program Files/LLVM/bin"
  NO_DEFAULT_PATH
)

find_program(
  llvm-lib
  NAMES llvm-lib
  REQUIRED
)

find_program(
  llvm-nm
  NAMESllvm-nm
  PATHS "C:/Program Files/LLVM/bin"
  NO_DEFAULT_PATH
)

find_program(
  llvm-nm
  NAMESllvm-nm
  REQUIRED
)

find_program(
  llvm-objdump
  NAMES clanllvm-objdump
  PATHS "C:/Program Files/LLVM/bin"
  NO_DEFAULT_PATH
)

find_program(
  llvm-objdump
  NAMES clanllvm-objdump
  REQUIRED
)

find_program(
  llvm-ranlib
  NAMES clallvm-ranlib
  PATHS "C:/Program Files/LLVM/bin"
  NO_DEFAULT_PATH
)

find_program(
  llvm-ranlib
  NAMES clallvm-ranlib
  REQUIRED
)

find_program(
  llvm-mt
  NAMESllvm-mt
  PATHS "C:/Program Files/LLVM/bin"
  NO_DEFAULT_PATH
)

find_program(
  llvm-mt
  NAMESllvm-mt
  REQUIRED
)

find_program(
  llvm-strip
  NAMES clllvm-strip
  PATHS "C:/Program Files/LLVM/bin"
  NO_DEFAULT_PATH
)

find_program(
  llvm-strip
  NAMES clllvm-strip
  REQUIRED
)

find_program(
  llvm-rc
  NAMESllvm-rc
  PATHS "C:/Program Files/LLVM/bin"
  NO_DEFAULT_PATH
)

find_program(
  llvm-rc
  NAMESllvm-rc
  REQUIRED
)
